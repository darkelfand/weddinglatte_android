<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!--
    툴바에 메뉴를 추가하고  싶을경우 여기에 등록한다.
    app:showAsAction
        naver 액션바아이템을 추가하여 표시하지 않음(디폴트)
        always 항상 액션바에 아이템을 추가하여 표시
        ifRoom 액션바에 여유 공간이 있을때만 아이템을 표시
        withText 타이틀 속성으로  설정된 제목을 같이 표시
        cdlapseActionView 아이템에 설정한 뷰의 아이콘만 표시

    android:orderInCategory 메뉴아이템 순서
    -->
    <item
        android:id="@+id/business_type"
        android:icon="@drawable/ic_tune_white_36dp"
        android:orderInCategory="100"
        android:title="@string/business_type"
        app:showAsAction="always" />
</menu>
